<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security" 
    xmlns:beans="http://www.springframework.org/schema/beans" 
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
    xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.2.xsd">
    <!-- HTTP security configurations -->
    <http auto-config="true" use-expressions="true" request-matcher="regex" >
        <form-login login-processing-url="/resources/j_spring_security_check" login-page="/login" authentication-failure-url="/login?login_error=t" />
        <logout logout-url="/resources/j_spring_security_logout" />
        <!-- Configure these elements to secure URIs in your application -->
        <intercept-url pattern="/resources/.*" access="permitAll" />
        
        <intercept-url pattern="\A/empresas\?form.*\Z" access="hasAnyRole('ADMINISTRADOR', 'GESTOR_ETT')" />
        <intercept-url pattern="\A/empresas\?page=\d*.*\Z" access="permitAll" />
        
        <intercept-url pattern="\A/usuarios\?form.*\Z" access="hasRole('ADMINISTRADOR')"/>      
        <intercept-url pattern="\A/usuarios\?page=\d*.*\Z" access="hasRole('ADMINISTRADOR')" /> 
		
		<intercept-url pattern="\A/ofertas\?form.*\Z" access="hasAnyRole('ADMINISTRADOR', 'GESTOR_EMPRESA')" />       
        <intercept-url pattern="\A/ofertas\?page=\d*.*\Z" access="permitAll" />
        
        <intercept-url pattern="\A/demandantes\?form.*\Z" access="permitAll" />
        <intercept-url pattern="\A/demandantes\?page=\d*.*\Z" access="hasAnyRole('ADMINISTRADOR', 'GESTOR_ETT', 'GESTOR_EMPRESA')" />
        
        <intercept-url pattern="\A/curriculums\?form.*\Z" access="hasRole('DEMANDANTE')" />
        <intercept-url pattern="\A/curriculums\?page=\d*.*\Z" access="hasRole('DEMANDANTE')" />
       
		<intercept-url pattern="\A/experiencias\?form.*\Z" access="hasRole('DEMANDANTE')" />
        <intercept-url pattern="\A/experiencias\?page=\d*.*\Z" access="hasRole('DEMANDANTE')" />       
       
        <intercept-url pattern="\A/puestoes\?form.*\Z" access="hasRole('DEMANDANTE', 'GESTOR_ETT', 'GESTOR_EMPRESA')" />
        <intercept-url pattern="\A/puestoes\?page=\d*.*\Z" access="hasRole('DEMANDANTE', 'GESTOR_ETT', 'GESTOR_EMPRESA')" />        
        
        <intercept-url pattern="\A/inscripcions\?form.*\Z" access="hasRole('DEMANDANTE', 'GESTOR_ETT', 'GESTOR_EMPRESA')" />
        <intercept-url pattern="\A/inscripcions\?page=\d*.*\Z" access="hasRole('DEMANDANTE', 'GESTOR_ETT', 'GESTOR_EMPRESA')" />        
        
        <intercept-url pattern="\A/localizacions\?form.*\Z" access="hasRole('DEMANDANTE', 'GESTOR_ETT', 'GESTOR_EMPRESA')" />
        <intercept-url pattern="\A/localizacions\?page=\d*.*\Z" access="hasRole('DEMANDANTE', 'GESTOR_ETT', 'GESTOR_EMPRESA')" />        
        
        <intercept-url pattern="\A/login.*\Z" access="permitAll" />
        <intercept-url pattern="/.*\Z" access="isAuthenticated()"/> 
    </http>
    <!-- Configure Authentication mechanism -->
    <authentication-manager alias="authenticationManager">
        <!-- SHA-256 values can be produced using 'echo -n your_desired_password | sha256sum' (using normal *nix environments) -->
        <authentication-provider>
			<jdbc-user-service data-source-ref="dataSource"
      		users-by-username-query="select username, password, enabled from usuario where username=?"
            authorities-by-username-query="SELECT username, rol from usuario WHERE username=?" />
  			<!--   <password-encoder hash="sha-256" />
  			<user-service>
            	<user name="admin" password="admin" authorities="ROLE_ADMIN" />
            </user-service>-->
		</authentication-provider>
    </authentication-manager>
</beans:beans>